-- Use roughly one-quarter of Omarchy's default window gaps (5px inner, 10px outer).
hl.config({
  general = {
    gaps_in = 1,
    gaps_out = {
      top = 0,
      right = 2,
      bottom = 2,
      left = 2,
    },
  },
})
