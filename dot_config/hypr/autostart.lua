-- Start the night-light filter with every Hyprland session.
o.launch_on_start("hyprsunset")

-- Keep the shell palette synchronized after unlocks and config reloads.
o.launch_on_start("omarchy-theme-resync")
