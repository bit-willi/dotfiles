# systemd-oomd only acts on units that opt in via ManagedOOM* properties.
# Without the drop-in under system/user@.service.d it runs but never kills
# anything, which is the state this machine was in.
[OOM]
DefaultMemoryPressureDurationSec=20s
