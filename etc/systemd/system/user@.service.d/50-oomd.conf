# 16 GB is not upgradable on this board, so a runaway browser process is the
# realistic way to run out of memory. Killing the worst offender beats letting
# the whole desktop stall on swap.
[Service]
ManagedOOMMemoryPressure=kill
ManagedOOMMemoryPressureLimit=60%
